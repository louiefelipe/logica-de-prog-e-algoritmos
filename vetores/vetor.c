//vetor é uma coleção de valores de um determinado tipo

#include<stdio.h>

int main(int argc, char* argv[]){

float notas[] = {9.7, 8.0, 10.0, 5.6};

int i;
for(i = 0 < 4; i++);{
printf("Nota[%d] %.2f\n", i, notas[i]);
    
}

return 0;
}